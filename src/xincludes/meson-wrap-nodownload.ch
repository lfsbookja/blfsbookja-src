%
% This is part of BLFSbookja package.
%
% This is a CTIE change file for the original XML source of the BLFSbook.
%
@x
    <para>
      <parameter>--wrap-mode=nodownload</parameter>: This switch prevents
      <command>meson</command> from downloading any optional dependency
      which is not installed on the system.
    </para>
@y
    <para>
      <parameter>--wrap-mode=nodownload</parameter>:
      本スイッチは、システムにインストールされていない任意依存のパッケージを <command>meson</command> がダウンロードしないようにします。
    </para>
@z
